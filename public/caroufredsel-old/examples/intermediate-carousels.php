<?php	$description = 'Intermatidate examples on how to create some of the most awesome jquery carousels.';	$keywords = 'intermediate, expert, fade, crossfade, carousel, continuously, endlessly, variable, dynamic, size, width, height, public, method';	$title = 'Intermediate carousels - Examples';	$script_foo1 = '$("#foo1").carouFredSel({	items		: 1,	scroll		: {		fx			: "crossfade"	},	auto		: false,	pagination	: {		container		: "#foo1_pag",		anchorBuilder	: function( nr ) {			var src = $("img", this).attr( "src" );				src = src.replace( "/large/", "/small/");			return \'<img src="\' + src + \'" />\';		}	}});';	$script_foo2 = 'var timerWidth = 870;$("#foo2").carouFredSel({	scroll	: 5,	auto	: {		pauseOnHover: "resume",		progress	: "#foo2_timer"	}});';	$script_foo3 = '$("#foo3").after(\'<ul id="fooX" />\').next().html($("#foo3").html());$("#foo3 li:odd").remove();$("#fooX li:even").remove();$("#foo3").carouFredSel({	synchronise	: "#fooX",	scroll		: 5});$("#fooX").carouFredSel({	auto		: false});';	$script_foo4 = 'var numberOfItems	= $("#foo4").children().length;var visibleItems	= 5;var missingItems	= visibleItems - (numberOfItems % visibleItems);for (var a = 0; a < missingItems; a++) {	$("#foo4").append( "<span style=\'display: block; float: left; width: 174px; height: 174px;\'></span>" );}$("#foo4").carouFredSel({	circular: false,	scroll	: {		duration		: 0.5,		timeoutDuration	: 2000	}});';	$scripts = '		<script type="text/javascript" language="javascript">			$(function() {				$("#foo1 .slide div").show();				'.$script_foo1.$script_foo2.$script_foo3.$script_foo4.'			});		</script>';	include_once '../functies/layout_pre.php';?>				<h1 style="display:none;">Intermediate carousels</h1>				<h3 id="_thumbnail_navigation">Thumbnail navigation</h3>				<p>Add thumbnails to navigate through the slides using the <code>atuo.anchorBuilder</code> function.<br />					Also, this carousel uses the <code>&quot;crossfade&quot;</code> effect.</p><?php	$E_script = $script_foo1;	$E_thumbnails = true;	$E_pagination = false;	include 'scripts/html_carousel.php';		if ( $showAds )	{		echo '				<div class="adsense_center_728">' . $adsense_balk . '				</div>';	}?>				<br />				<br />				<h3 id="_timer">Timer</h3>				<p>Set a timer to visualize the remaining time before scrolling again.</p><?php	$E_script = $script_foo2;	include 'scripts/list_carousel.php';?>				<br />				<br />				<h3 id="_multiple_row">Multiple-row carousel</h3>				<p>A programmable multiple-row carousel is not supported, but a similar effect can be created using some smart workaround:</p>				<ul class="no_marg">					<li>devide the items into two carousels</li>					<li>scroll both carousels simultaneously</li>				</ul><?php	$E_script = $script_foo3;	$E_element = 'ul';	$E_list = 'abcdefghijklmnopqrstuvwxyz';	include 'scripts/list_carousel.php';?>				<br />				<br />				<h3 id="_fill_last_page">Not filling the Last page with items from the previous page.</h3>				<p>In a non-circular carousel -if the amount of images is not an exact multiplecation of the number of visible items- the last page will be filled with the last items of the previous page. A workaround to prevent this behavior (and only show the items of the last page) could be to add the number of missing items with non-visible elements.</p><?php	$E_script = $script_foo4;	include 'scripts/image_carousel.php';	if ( $showAds )	{		echo '				<div class="adsense_center_728">' . $adsense_balk . '				</div>';	}?>				<br /><?php	include 'index.php';	include_once '../functies/layout_post.php';?>